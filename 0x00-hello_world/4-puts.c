#include <stdio.h>
/**
 *main- Serves as the entry point for the program
 *
 *Return:default's return 0 if no errors else return non zero value.
 */
int main(void)
{
  puts("\"Programming is like building a multilingural puzzle");
  return (0);
}
