#include <stdio.h>
/**
 *main-Serves as the entry point for the program
 *
 *Return: default returns zero if no errors else return a non zero value.
 */
int main(void)
{
  puts("\"Programming is like building a multilingual puzzle");
  return (0);
}
