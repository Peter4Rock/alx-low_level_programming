#include <stdio.h>
/**
 *main- Serves as the entry point for program
 *
 *Return: By default return zero if no errors else returns non-zero value.
 */
int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
